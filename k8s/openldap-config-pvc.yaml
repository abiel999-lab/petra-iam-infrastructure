apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: microk8s-hostpath
